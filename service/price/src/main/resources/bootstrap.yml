server:
  port: 17002
spring:
  application:
    name: price
  profiles:
    active: dev
  cloud:
    config:
      profile: dev
      uri: http://127.0.0.1:8888/
      discovery:
        enabled: true
        service-id: configserver